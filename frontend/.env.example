# Backend origin — do NOT include a path. The app appends /api/v1 itself.
VITE_API_URL=http://localhost:5001

# Google OAuth client ID (authorization-code flow)
VITE_GOOGLE_CLIENT_ID=your_google_oauth_client_id
